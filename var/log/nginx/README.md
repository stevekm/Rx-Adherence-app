This is the default error.log location when running nginx, in relation to the install directory 'prefix', and is used when running e.g. `nginx -t` to print log messages before custom config is loaded
